// Bai 1
const Math = () => { };

Math.add = (x, y) => {
    return x + y;
}

Math.sub = (x, y) => {
    return x - y;
}

Math.mult = (x, y) => {
    return x * y;
}

Math.div = (x, y) => {
    return x / y;
}

module.exports = Math;